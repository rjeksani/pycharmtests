# this is similar to list, but cannot be modified (updated - inserted or deleted at runtime). "(" is used to assign

sampleList = (1, 2, "raghav", 0.089) # Multiple data types in a single tuple

print(sampleList)
print(sampleList[3])  # printing an index
print(sampleList[-1])   # last index is printed

